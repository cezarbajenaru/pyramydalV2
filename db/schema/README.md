# Schema SQL (reference)

These files are the **baseline** applied by Alembic revision `0001`.

Do **not** edit them for new schema changes. Add a new revision under `db/alembic/versions/` instead:

```bash
.venv/bin/python -m alembic revision -m "describe_change"
```
